---
name: write-notion-content
description: Style and format rules for writing content to Notion. Use whenever creating or editing any Notion content (pages, ticket bodies, comments). Governs how the text reads, not status fields or MCP plumbing. Enforces template adherence, brevity, lists over prose, and no em-dashes.
---

# write-notion-content

Rules for how content reads when written to Notion. Style only. This skill does
not move status fields, create pages, or make MCP calls.

## Rules

1. **Follow the template.** If the page or ticket has an existing template or
   structure, match its format exactly. Fill its sections; do not reshape them.
2. **Do not create new sections.** Highly discouraged. Use what the template or
   page already has. Add a section only if the user explicitly asks.
3. **Few words.** Cut filler, articles, and pleasantries. Why speak many word
   when few word do trick.
4. **Lists are fine.** Prefer a bullet list over a paragraph when listing things.
5. **One sentence is ideal.** Say it in one sentence where possible.
6. **No em-dashes.** Never use em-dashes. Use a period, comma, or parentheses.
